import styled from "@emotion/styled";
import { Menu } from "antd";

export const StyledMenu = styled(Menu)<any>`
  padding: 5px;
  border-radius: 6px;
`;

export const StyledMenuItem = styled(Menu.Item)`
  margin-bottom: 5px;
`;
